"""
channels — Multi-Channel Messaging Connector System
====================================================
Abstract channel protocol + concrete implementations for all major
messaging platforms. Every inbound message passes through the governed
pipeline: PolicyEngine → drift check → ProofVault audit.

Surpasses OpenClaw by governing every message through ELFE convergence.
"""
